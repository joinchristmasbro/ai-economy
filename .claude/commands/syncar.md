---
description: commit + push manual pro GitHub (git já é automático via hook Stop)
---

Execute the skill named `syncar` located at `.claude/skills/syncar/SKILL.md`.

Read `.claude/skills/syncar/SKILL.md` first, then follow its instructions exactly using the user's arguments below (if any):

$ARGUMENTS
